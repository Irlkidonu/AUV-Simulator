[pytest]
testpaths = test
markers =
    gating: validation tests that gate a paper claim. If one fails, the affected
        claim is dropped rather than rescued by retuning the physics
        (OPTICAL_PROPAGATION_SPEC.md section 10).
